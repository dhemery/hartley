# Expressions for Java 8

-   Expressions use Function instead of Feature.
-   Expressions use Consumer instead of Action.
-   Expressions use Predicate
    -   Instead of Matcher?
    -   In addition to Matcher?

